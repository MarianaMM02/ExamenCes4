// eslint-disable-next-line react/prop-types
const Boton = ({onClick}) => {
      return (
        <div>
          <button onClick={onClick}>Calcular</button>
        </div>
      );
    };
    
    export default Boton;